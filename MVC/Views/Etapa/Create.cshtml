@model MVC.Models.EtapaViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Alta Etapa</h1>

@*<h4>EtapaViewModel</h4>*@
<hr />
@if (ViewData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @ViewData["ErrorMessage"]
    </div>
}
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="form-group">
                <label asp-for="EtapaNombre" class="control-label"></label>
                <input asp-for="EtapaNombre" class="form-control" />
                <span asp-validation-for="EtapaNombre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="hidden" name="Operacion" value="Alta" />
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Regresar a la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
